{
  "name": "rivian-software-updates",
  "version": "2.13.1",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
