{
  "name": "rivian-software-updates",
  "version": "2.25.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "pdfjs-dist": "^5.6.205"
  }
}
