{
  "name": "rivian-software-updates",
  "version": "1.5.2",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
